import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "@/components/ui/sonner";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Quiz Challenge - Test Your Knowledge",
  description: "Interactive quiz application with 15 questions, 30-minute time limit, and detailed results. Test your knowledge across various categories!",
  keywords: "quiz, trivia, test, knowledge, education, learning",
  authors: [{ name: "Quiz App" }],
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster 
          richColors 
          position="top-right"
          expand={false}
          closeButton
        />
      </body>
    </html>
  );
}
